#include <iostream>
#include <cmath>
#include <algorithm>
using namespace std;
struct island {
    int l,r;
}islands[1005];
bool cmp(island x,island y) {
    return x.r < y.r;
}
int main() {
    int n,d;
    cin>>n>>d;

    
    return 0;
}